apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: pod-reader
  namespace: otherns
rules:
- apiGroups: [""]
  resources:
    ["pods", "pods/log"]
  verbs: # РАЗРЕШЕННЫЕ ДЕЙСТВИЯ:
    ["get", "list", "watch", "describe"]